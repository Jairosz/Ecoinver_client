// src/app/types/LoginResponse.ts
export interface LoginResponse {
    // Ajusta las propiedades según la respuesta real de tu backend
    token: string;
    userId?: string;
    UserName: string;
    // otras propiedades opcionales...
  }
